spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///ssm?useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #日志配置

#mybatis-plus:
#  configuration:
#  map-underscore-to-camel-case: false # 数据库下划线自动转驼峰标示关闭

  #&serverTimezone=Asia/Shanghai  mysql8